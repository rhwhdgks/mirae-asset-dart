[pytest]
testpaths = tests
python_files = test_*.py
addopts = -p no:cacheprovider
markers =
    canonical: out/canonical 정본(Parquet)·검색 인덱스가 필요한 테스트 — make pytest-canonical
    canonical_server: ServerRuntime 실행 스레드에서 정본을 만지는 테스트 — 다른 정본 테스트와 한 프로세스에 섞지 않는다 (make server-api-test)
